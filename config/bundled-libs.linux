# Non-system shared libraries copied from the build host into runtime/lib
# (by SONAME, symlinks dereferenced). Everything else observed in the
# dynamic linkage must match config/dependency-allowlist.linux or the
# build fails. Adding a line here is a reviewed change.
libz.so.1
liblz4.so.1
libzstd.so.1
